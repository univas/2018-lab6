export class Subject {

    id : number;

    name : string;

    course : string;

    constructor(id?: number, name?: string, course?: string) {
        this.id = id;
        this.name = name;
        this.course = course;
    }

}